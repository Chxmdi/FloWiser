# Seed scripts

This directory is reserved for tenant, branch, site, and demo-data seed helpers.

Epic 1 only creates the folder and documents the intent so later epics can add repeatable onboarding scripts without changing the repository structure again.
